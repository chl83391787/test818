# 학생 제출 산출물

> 이 문서 하나만 작성하여 코드·스크린샷 링크와 함께 제출합니다. 대괄호 부분을 본인의 내용으로 바꾸세요.

## 1. 제출 정보

| 항목 | 작성 내용 |
|---|---|
| 이름/학번 | 최지혜 |
| 수행 일자 | 2026-08-18 |
| GitHub 저장소 | https://github.com/chl83391787/test818 |
| 작업 브랜치 | `feature/css-refactor` |
| Pull Request | https://github.com/chl83391787/test818/pull/1 |
| 최종 커밋 | `e38dcb4` |

## 2. CSS 분리 결과

| 순서 | 파일 | 실제로 옮긴 주요 선택자·역할 | 확인 |
|---:|---|---|---|
| 1 | `nomar.css` | 폰트 선언(`@font-face` Pretendard 400~700), 기본 reset 및 HTML 태그 스타일 초기화(`html`, `body`, `*`, `ul`, `p`, `h1~h6`, `a`, `button`, `img`, `input`) | [x] |
| 2 | `root.css` | 색상, 고정/적응형 폰트 크기, 폰트 굵기, spacing scale 단위(`--num0`~`--num160`), 전역 그리드 변수 등 원시 CSS 변수(`:root`) 선언 | [x] |
| 3 | `component.css` | 프로젝트 의미 기반 지역 디자인 토큰(`--main`, `--ink`, `--line`, `--radius-*`, `--text-*`, `--padding-*`, `--icon-*`, `--bd-radius*` 등) | [x] |
| 4 | `btn_cpn.css` | 버튼 컴포넌트(`.btn-XS-color`, `.btn-XS-Ncolor`, `.btn-more`, `.tab-btn`, `.slide-arrow`, `.scroll-top-btn`), 아이콘(`.icon-*`, `.cart-icon`, `.cart-count` 등) | [x] |
| 5 | `item_cpn.css` | 텍스트/색상 유틸리티(`.col-*`, `.*-text-cpn`, `.section-title`, `.section-sub`), 상품 정보(`.product-code`, `.product-price`), 스와치(`.swatch-list`, `.swatch`) | [x] |
| 6 | `layuit_cpn.css` | 공통 컨테이너(`.con`), Grid 시스템(`.grid-2~5`), 섹션 구조(`.site-header`, `.hero`, `.quick-links`, `.collection`, `.outlet`, `.trending`, `.promo`, `.lookbook`, `.site-footer`, `.mobile-nav`) | [x] |
| 7 | `style.css` | 페이지 특화 개별 스타일(`.collection-media img`, `.promo-card` 둥글기, `.product-name` 2줄 말줄임, `.trending-promo` 카드 레이아웃) | [x] |
| 8 | `media.css` | 1600px, 1024px, 768px, 480px 미디어 쿼리 반응형 레이아웃 규칙 및 모바일 메뉴 토글 인터랙션 | [x] |

### index.html 연결 순서

```html
<link rel="stylesheet" href="./css/nomar.css">
<link rel="stylesheet" href="./css/root.css">
<link rel="stylesheet" href="./css/component.css">
<link rel="stylesheet" href="./css/btn_cpn.css">
<link rel="stylesheet" href="./css/item_cpn.css">
<link rel="stylesheet" href="./css/layuit_cpn.css">
<link rel="stylesheet" href="./css/style.css">
<link rel="stylesheet" href="./css/media.css">
```

## 3. 오류 수정 보고

### 결함 1

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-01 |
| 선택자 | `.grid-5` / `.product-grid` |
| 문제 선언 | `.grid-5 { display: grid; grid-template-columns: repeat(5 1fr); gap: var(--num20); }` (repeat 인자 내 쉼표 누락 문법 오류) |
| 화면 증상 | BEST PICKS 영역의 5개 상품 카드가 가로 5열 그리드로 정렬되지 못하고 세로 1열로 떨어지거나 레이아웃이 무너짐 |
| 진단 방법 | 개발자 도구(F12) Elements > Styles 패널에서 `.grid-5`의 `grid-template-columns: repeat(5 1fr)`에 쉼표(`,`)가 누락되어 Invalid property value 오류로 속성이 무효화된 것을 확인 |
| 수정 선언 | `.grid-5 { display: grid; grid-template-columns: repeat(5, 1fr); gap: var(--num20); }` 및 `.product-grid { display: flex; justify-content: space-between; }` |
| 수정 전/후 증빙 | 수정 전: 쉼표 누락으로 5단 그리드가 깨져 세로 나열됨 / 수정 후: 정상 5열 가로 그리드 배치 완료 |

### 결함 2

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-02 |
| 선택자 | `.con` / `.collection-media img` / `.promo-card` |
| 문제 선언 | `.con { margin: var(--G-center); max-width: var(--containner); ... }` (CSS 변수명 오타: `--container`가 아닌 `--containner` 선언) |
| 화면 증상 | 1920px 등 대화면에서 콘텐츠 최대 너비가 1600px로 제한되지 않고 100%로 늘어나 중앙 정렬이 풀림 (추가로 시안 대비 메인 이미지 모서리가 둥글지 않음) |
| 진단 방법 | 개발자 도구(F12) Computed 패널에서 `.con`의 `max-width`가 `none`으로 계산됨을 확인하고, Styles 패널에서 `--containner` 변수명 오타를 식별함 |
| 수정 선언 | `.con { margin: var(--G-center); max-width: var(--container); padding-left: var(--padding-grid-LARGE); padding-right: var(--padding-grid-LARGE); }` 및 `.collection-media img, .promo-card { border-radius: 30px; }` |
| 수정 전/후 증빙 | 수정 전: 변수 오타로 1920px 대화면에서 콘텐츠가 좌우로 퍼짐 / 수정 후: 최대 폭 1600px 고정 및 중앙 정렬, 이미지 둥글기 시안 일치 완료 |

### 결함 3

| 항목 | 작성 내용 |
|---|---|
| 결함 ID | BUG-03 |
| 선택자 | `body` |
| 문제 선언 | `body { color: var(--ink); background-color: var(--color-white); word-break: keep-all; min-width: 1600px; }` (min-width: 1600px 고정) |
| 화면 증상 | 1600px 미만의 태블릿·모바일 화면(1024px, 768px, 480px)에서 화면 전체에 불필요한 하단 가로 스크롤바가 발생하고 반응형 축소가 정상 작동하지 않음 |
| 진단 방법 | 개발자 도구(F12) 반응형 모드에서 1600px 미만으로 뷰포트 축소 시 하단 가로 스크롤 확인, Elements > Computed 패널에서 `body`의 `min-width: 1600px` 고정 선언 확인 |
| 수정 선언 | `body` 선언부에서 `min-width: 1600px;` 속성을 제거하여 뷰포트 너비에 맞추어 유연하게 줄어들도록 수정 |
| 수정 전/후 증빙 | 수정 전: 1600px 미만 모든 해상도에서 가로 스크롤바 발생 / 수정 후: 1024px, 768px, 480px 전 구간에서 가로 스크롤 없이 완벽한 반응형 렌더링 확인 |

## 4. 반응형·회귀 테스트

| 테스트 ID | 너비 | 확인 내용 | 결과 | 증빙 |
|---|---:|---|---|---|
| TC-01 | 1920px | 컨테이너 최대 폭·중앙 정렬 | [통과/실패] | [링크] |
| TC-02 | 1600px | 좌우 여백 전환 | [통과/실패] | [링크] |
| TC-03 | 1024px | 모바일 메뉴·3단 상품 Grid·가로 스크롤 없음 | [통과/실패] | [링크] |
| TC-04 | 768px | 2단 Grid·컬렉션 세로 배치·body 너비 | [통과/실패] | [링크] |
| TC-05 | 480px | 모바일 간격·글자·푸터·가로 스크롤 없음 | [통과/실패] | [링크] |
| TC-06 | 전체 | CSS 404·콘솔 오류 없음 | [통과/실패] | [링크] |

## 5. Git/GitHub 작업 기록

| 순서 | 커밋 | 메시지 | 작업 내용 |
|---:|---|---|---|
| 1 | `6a7f148` | `chore: add initial problem files` | 원본 문제 코드(index.html, style.css) 저장소 추가 및 초기 커밋 생성 |
| 2 | `6a4a933` | `refactor: split base tokens and components` | style.css에서 nomar, root, component, btn_cpn, item_cpn 파일 분리 |
| 3 | `209f1fd` | `refactor: separate layout and responsive styles` | layuit_cpn, style, media 파일 분리 및 index.html 8개 CSS 연결 |
| 4 | `e38dcb4` | `fix: correct three layout defects` | .con 최대폭 오타(--containner), .grid-5 문법(repeat(5, 1fr)), body min-width 결함 수정 |
| 5 | `latest` | `docs: add test evidence and report` | 03_학생_제출_산출물.md 작성 및 반응형 화면 검증 결과 보고서 완료 |

### PR 검토 반영

- 받은 의견: CSS 파일 분리 후 로드 순서에 따른 우선순위(캐스케이딩) 정상 동작 여부 및 1600px 미만 가로 스크롤 이슈 해결 검토 요청
- 변경한 내용: `nomar.css`부터 `media.css`까지 8개 CSS 파일의 `<link>` 연결 순서를 정리하고, `body`의 `min-width: 1600px`를 제거하여 전 해상도 가로 스크롤 방지
- 재검증 결과: 1920px, 1600px, 1024px, 768px, 480px 전 뷰포트에서 가로 스크롤 없이 정상 렌더링 확인 완료

## 6. 최종 회고

- 가장 찾기 어려웠던 오류와 이유: `.con` 선택자의 `max-width: var(--containner);` 변수명 오타(n이 2개) 오류였습니다. CSS 변수 오타는 브라우저 콘솔에 별도의 에러를 발생시키지 않고 조용히 속성 적용을 무시하기 때문에, 개발자 도구의 Styles 패널과 변수 선언부(:root)의 철자를 꼼꼼하게 대조하여 원인을 파악해야 했습니다.
- CSS 파일 분리 기준을 정한 방법: 역할과 관심사 분리(SoC) 원칙에 따라, 전역 리셋/폰트(`nomar.css`), 원시 디자인 변수(`root.css`), 의미 기반 컴포넌트 변수(`component.css`), 공통 버튼/아이콘(`btn_cpn.css`), 텍스트 유틸/상품 아이템(`item_cpn.css`), 공통 그리드/레이아웃(`layuit_cpn.css`), 페이지별 특화 스타일(`style.css`), 반응형 미디어 쿼리(`media.css`)의 8개 레이어로 구분하여 구성했습니다.
- 파일 분리 후 캐스케이드 문제를 예방한 방법: CSS의 선언 순서에 따른 우선순위 규칙을 준수하여 `index.html` 상단부터 기본 토큰/리셋 -> 컴포넌트 -> 레이아웃 -> 페이지 스타일 -> 미디어 쿼리 순서대로 `<link>` 태그를 배치함으로써 스타일 간 간섭과 예기치 않은 덮어쓰기 문제를 방지했습니다.
- 다시 수행한다면 개선할 점: 파일명 오타(`nomar.css` -> `normal.css`, `layuit_cpn.css` -> `layout_cpn.css`)를 표준적인 네이밍으로 리팩토링하고, 반복되는 스타일 유틸리티 클래스를 더욱 체계화하여 모듈화 완성도를 높이고자 합니다.

## 7. 최종 확인

- [x] 정답 코드를 복사하지 않고 직접 수행했다.
- [x] HTML 콘텐츠를 임의 삭제하지 않았다.
- [x] CSS 파일 8개와 연결 순서를 확인했다.
- [x] 세 결함의 원인·수정·증빙을 기록했다.
- [x] 실패한 테스트를 숨기지 않았다.
- [x] 저장소와 PR을 강사가 열람할 수 있다.
